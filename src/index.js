import { init, isSupported, getRenderer } from './context.js';
import { arrayN, array2, array3 } from './array.js';

export { init, isSupported, getRenderer };
export { arrayN, array2, array3 };

export default { init, isSupported, getRenderer, arrayN, array2, array3 };
