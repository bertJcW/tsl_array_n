{
  "name": "tsl-array-n-monorepo",
  "private": true,
  "description": "Monorepo root — see packages/tsl_array_n (published) and, later, packages/fluxflow.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w tsl_array_n",
    "test": "npm run test --workspaces --if-present"
  },
  "license": "MIT"
}
